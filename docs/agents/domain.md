# Domain docs

Single-context layout: one `CONTEXT.md` and one `docs/adr/` directory at the repo root. Created with [ADR 1](../adr/0001-audio-playback-no-howler.md) (M5/ENG-70's audio-plumbing slice) — the first architecture decision to reach code. `CONTEXT.md` is the stable orientation map; `docs/adr/` is the append-only decision log, one numbered file per decision.
