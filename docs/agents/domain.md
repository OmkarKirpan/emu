# Domain docs

Single-context layout: one `CONTEXT.md` and one `docs/adr/` directory at the repo root, once there's a codebase to document. Neither exists yet — this repo is still in the planning phase (see the wayfinder map linked from `CLAUDE.md`); create them when the first architecture decision records reach code, not before.
