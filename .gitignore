# Zig
.zig-cache/
zig-out/

# Node / Vite
node_modules/
dist/
dist-ssr/
*.local

# Commercial game ROMs, never committed. This repo vendors only test ROMs
# whose redistribution was researched (docs/research/test-rom-licensing.md)
# plus its own hand-written `sprite_input_demo.nes`; a real game dropped in
# here for local testing must not reach a public repo by way of a stray
# `git add -A`. Keep personal ROM libraries outside the tree entirely --
# this is a backstop, not the plan.
docs/roms/

# Agent worktrees (transient, created by the Agent tool)
.claude/worktrees/

# ENG-78: the fetched SingleStepTests/65x02 data set (1.08GB, never
# vendored) -- see core/tools/fetch-65x02.sh.
core/.cache/
